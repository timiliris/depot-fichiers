# Holds the cookie signing key and the password hashes.
gateway/config.json
config.yaml

# Host-specific files derived from the templates
sftp/sshd_sftp_config
docker-compose.override.yml
.env

# Binaries
depot-gw
*.img
